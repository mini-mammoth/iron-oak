// The root project builds nothing itself. Each subproject has its own build.gradle:
// common/ — loader-neutral (empty for now, phase A of #76), fabric/ — everything the mod
// is today. See settings.gradle and docs/ops/multiloader.md.
